[project]
name = "crud-fastapi"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]",
    "sqlalchemy",
    "pydantic[email]",
    "bcrypt",
    "python-multipart"
]
